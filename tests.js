function wasiII(vocabScore, matrixScore, age) {
    // lookup vocabTScore and matrixTScore from table
    
    // select t_score from wasi_ii_tscore where vc = vocabTScore and mr = matrixTScore and
    
    // sum into sumOfTScores
    // use table to lookup composite score, percentile rank, 95% confidence interval
    // return these values
}
